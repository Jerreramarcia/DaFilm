El indice se creará con el cóigo almacenado en la carpeta "_codigo", mientras que la interfaz gráfica se compila en la carpeta "interfaz"
El indice se almacenará en la carpeta de la interfaz (interfaz/_codigo/index/)

Perdón por el peso de los archivos pero no se si ibas a necesitar los JARs